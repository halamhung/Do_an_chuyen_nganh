@model HKQTravellingAuthenication.Models.Tour.StartLocations

@{
    ViewData["Title"] = "Loại bỏ điểm đi";
    Layout = "_Layout";
}
<style>
    .dl-horizontal {
        display: flex;
        flex-wrap: wrap;
    }

        .dl-horizontal dt {
            flex-basis: 30%; /* Điều chỉnh độ rộng của các phần tử dt */
            flex-shrink: 0;
            margin-right: 15px; /* Khoảng cách giữa các phần tử dt và dd */
        }

        .dl-horizontal dd {
            flex-basis: 70%; /* Điều chỉnh độ rộng của các phần tử dd */
            flex-shrink: 0;
        }
</style>

<div class="tile">
    <h3 class="tile-tile">Bạn có chắc chắn là xóa điểm khởi hành này?</h3>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StartLocationName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StartLocationName)
        </dd>
    </dl>
    
    <form asp-action="Delete" asp-controller="StartLocationsAdministrator" asp-area="Tour" method="post">
        <input type="hidden" asp-for="StartLocationId" />
        <button class="btn btn-save" value="Delete" type="submit">Xóa</button>
        <a class="btn btn-cancel" href="@Url.Action("Index", "StartLocationsAdministrator", new { area = "Tour" })">Hủy bỏ</a>
    </form>
</div>
